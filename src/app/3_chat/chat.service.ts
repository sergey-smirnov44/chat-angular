/*
import { Store } from '@ngrx/store';
import { Message } from 'src/app/core/common/3_chat/messageChat.interface';
import { Date } from 'src/app/core/common/3_chat/dateChat.interface';
import { Injectable } from '@angular/core';
import * as fromChat from './chat.reducer';
import * as chatAction from './chat.actions'

@Injectable()
export class MessageService {
  constructor(private store: Store<fromChat.Chat>) {}

  sendMessage(selectedId: string) {
    this.store.dispatch(new chatAction.SendMessage)
  }
}

*/
